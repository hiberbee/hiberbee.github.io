---
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" {% if site.lang %}xml:lang="{{ site.lang }}"{% endif %}>
    <generator uri="https://jekyllrb.com/" version="{{ jekyll.version }}">Jekyll</generator>
    <link href="{{ page.url | absolute_url }}" rel="self" type="application/atom+xml"/>
    <link href="{{ '/' | absolute_url }}" rel="alternate" type="text/html" {% if site.lang %}hreflang="{{ site.lang }}" {% endif %}/>
    <updated>{{ site.time | date_to_xmlschema }}</updated>
    <id>{{ page.url | absolute_url | xml_escape }}</id>
    {% assign title = site.title | default: site.name %}
{% if title %}
    <title type="html">{{ title | smartify | xml_escape }}</title>
{% endif %}
{% if site.description %}
    <subtitle>{{ site.description | xml_escape }}</subtitle>
{% endif %}
{% if site.author %}
        <author>
            <name>{{ site.data.author.name | default: site.author | xml_escape }}</name>
            {% if site.data.author.email %}
            <email>{{ site.data.author.email | xml_escape }}</email>
            {% endif %}
            {% if site.data.author.uri %}
            <uri>{{ site.data.author.uri | xml_escape }}</uri>
            {% endif %}
        </author>
{% endif %}
    {%- assign events = site.events | where_exp: "item", "item.date >= site.time" | sort: "date" | reverse -%}
    {% for post in events %}
        {%- assign t = site.trainings | where_exp: "item", "item.title == post.title" | first -%}
    <entry{% if post.lang %}{{ " " }}xml:lang="{{ post.lang }}"{% endif %}>
        <title type="html">{{ site.trainings_identity_map[post.slug] | smartify | strip_html | normalize_whitespace | xml_escape }}</title>
        <link href="{{ site.url }}/trainings/{{ training.slug }}/" rel="alternate" type="text/html" title="{{ site.trainings_identity_map[post.slug] | xml_escape }}"/>
        <published>{{ post.date | date_to_xmlschema }}</published>
        <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
        <id>{{ post.id | absolute_url | xml_escape }}</id>
        <content type="html" xml:base="{{ site.url }}/trainings/{{ t.slug }}/">
            {{ t.content | strip_html | xml_escape }}
        </content>
        {%- for category in t.categories %}
            <category term="{{ category | xml_escape }}"/>
        {% endfor %}
        {%- for tag in t.tags %}
            <category term="{{ tag | xml_escape }}"/>
        {% endfor %}
    </entry>
    {% endfor %}
</feed>
